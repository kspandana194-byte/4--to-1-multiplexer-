# 4--to-1-multiplexer-
4-to-1 Multiplexer (MUX) is a digital circuit that selects one of four input signals and sends it to a single output.It uses two select lines to determine which input is connected to the output.
